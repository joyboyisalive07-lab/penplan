"""Calibration profiles shipped with the tool.

The JSON files beside this one were made by running the calibration wizard
against the real sites, on a 1920x1080 display at 100 per cent scaling. They
are a starting point, not a guarantee: a canvas moves when the window is
resized, so recalibrating takes a minute and removes all doubt.
"""
